from setuptools import setup

setup(name='csv_combinator',
      version='0.1',
      description='Combines CSVs',
      author='Bart Lanen',
      license='MIT',
      packages=['csv_combinator'],
      zip_safe=False)